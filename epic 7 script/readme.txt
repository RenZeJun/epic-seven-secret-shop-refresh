type these in cmd:
pip install pyperclip (press enter)
pip install xlrd==1.2.0 (press enter)
pip install pyautogui==0.9.50 (press enter)
pip install opencv-python -i https://pypi.tuna.tsinghua.edu.cn/simple (press enter)
pip install pillow (press enter)

open your cmd at the right directory, use py secret shop.py to run the script